// firebaseReducer.js
export default (state, action) => {
    switch (action.type) {
      // Aquí puedes manejar diferentes tipos de acciones como "SET_MENU", "ADD_ITEM", etc.
      default:
        return state;
    }
  };
  